# 00_container_mode.yml
---
- name: Enable container mode
  hosts: mikrotik
  gather_facts: no
  connection: ansible.netcommon.network_cli
  vars:
    ansible_network_os: community.routeros.routeros
    ansible_command_timeout: 60
    usb_path: "usb1"

  tasks:
    - name: Configure container registry and tmpdir
      community.routeros.command:
        commands:
          - /container/config/set registry-url=https://registry-1.docker.io tmpdir={{ usb_path }}/docker/pull

    - name: Enable container mode (REQUIRES RESET BUTTON!)
      community.routeros.command:
        commands:
          - /system/device-mode/update container=yes
      ignore_errors: yes

    - name: IMPORTANT
      debug:
        msg: |
          ==========================================
          ТРЕБУЕТСЯ ДЕЙСТВИЕ!
          В течение 5 минут нажми кнопку RESET
          (короткое нажатие, не удерживать!)
          Роутер перезагрузится автоматически.
          ==========================================